# A-SIMPLE-AJAXCRUD-MVC

To view that, you have to config project in the way "source/Config.php" and you have to use the dump of the database in source ('raiz, na mesma pasta que o composer.json está')
